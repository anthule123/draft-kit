

import Image from "next/image";
import logoSrc from "@/app/na/hello.svg";

    <div style={{ border: "1px solid black" }}>
      <Image
        src={logoSrc}
        alt="Logo"
        width={500}
        height={500}
        style={{ width: "auto", height: "auto" }}
      />
    </div>

```math
L = 1/2 rho v^2 S C_L

#align(center)[
  $
  a &= 3 \
  b &= 4 \
  c &= "ssdf" + d
  $
]
```
